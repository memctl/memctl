# Database
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Auth
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000

# Dev auth bypass (local only; no GitHub OAuth required)
DEV_AUTH_BYPASS=false
NEXT_PUBLIC_DEV_AUTH_BYPASS=false
DEV_AUTH_BYPASS_ORG_SLUG=dev-org
NEXT_PUBLIC_DEV_AUTH_BYPASS_ORG_SLUG=dev-org
DEV_AUTH_BYPASS_USER_EMAIL=dev@local.memctl.test
DEV_AUTH_BYPASS_USER_NAME=Dev User
DEV_AUTH_BYPASS_ADMIN=false

# Self-hosted mode (disables billing, unlocks all limits)
SELF_HOSTED=false
NEXT_PUBLIC_SELF_HOSTED=false

# Dev plan override (development only)
# DEV_PLAN=enterprise

# Stripe
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_LITE_PRICE_ID=
STRIPE_PRO_PRICE_ID=
STRIPE_BUSINESS_PRICE_ID=
STRIPE_SCALE_PRICE_ID=

# Stripe metered billing (optional, for enterprise memory metering)
STRIPE_METERED_PRICE_ID=

# Cron secret (for /api/cron/* endpoints)
CRON_SECRET=

# MCP Server
MEMCTL_API_URL=http://localhost:3000/api/v1

# GitHub API (optional, for landing page stats)
GITHUB_TOKEN=

# Email (Resend) — optional in dev, emails log to console without it
RESEND_API_KEY=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
